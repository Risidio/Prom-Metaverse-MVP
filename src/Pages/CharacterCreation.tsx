import RedButton from "../Components/Buttons/RedButton";

const CharacterCreation = () => {
  return (
    <RedButton pathLink="/onboarding"
      text="Create my character"
      className="button--character"></RedButton>
  );
}

export default CharacterCreation;